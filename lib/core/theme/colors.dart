// LiteLauncher 主题常量
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // 背景
  static const background = Color(0xFF0D1117);
  static const surface = Color(0xFF161B22);
  static const surfaceLight = Color(0xFF21262D);

  // 强调色
  static const accentCyan = Color(0xFF00E5FF);
  static const accentPurple = Color(0xFF7C4DFF);

  // 文字
  static const textPrimary = Color(0xFFC9D1D9);
  static const textSecondary = Color(0xFF8B949E);

  // 语义色
  static const success = Color(0xFF3FB950);
  static const warning = Color(0xFFD29922);
  static const error = Color(0xFFF85149);

  // Loader 徽章色
  static const fabric = Color(0xFF3FB950);
  static const quilt = Color(0xFF9C27B0);
  static const forge = Color(0xFFFF9800);
  static const neoforge = Color(0xFFF44336);
  static const optifine = Color(0xFFFFEB3B);
}
